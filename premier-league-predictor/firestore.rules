rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows all reads and writes to all collections
      allow read, write: if true;
    }
  }
}